<h2 class="mt-3" >List of Attendees For The Upcoming Events</h2>
<div class="card w-75 mt-4 ">
  <h3 class="card-header fw-bold text-secondary">
  Event: <%= @event.description%></h3>
  <div>
    <h6 class='btn btn-outline-dark m-1 w-23 p-2' ><%=@event.event_date%></h6> 
    <h6 class='btn btn-outline-dark m-1 w-25 p-2'>Created by:<%=@event.creator.username%></h6>
  </div>
  <div class="card-body">
     <% @event.attended_events.each do |e|%>
    <div class="border border-secondary mb-2 rounded p-3">
      <h4>Attendee: <%= e.user.username %></h4>
      <h5><%= e.user.email %></h5>
    </div>
     <%end%>
  </div>
</div>